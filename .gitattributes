# Indexed mzML files are seeked by byte offset; CRLF conversion on checkout
# (Windows default) shifts every offset and breaks parsing. Pin them to LF.
*.mzML text eol=lf
*.gz binary

# Unified patch context lines retain their required leading space.
docs/psi-ms-submission/psi-ms-CV.patch whitespace=-blank-at-eol
